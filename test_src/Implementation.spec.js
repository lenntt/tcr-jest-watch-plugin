const Implementation = require("./Implementation")

describe('Implementation', () => {
    describe('the answser', () => {
        it('is 42', () => {
            expect(Implementation.answer).toEqual(42)
        })
    })
})